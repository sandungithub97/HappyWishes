NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Host-only secret for /rsvp-inbox and GET /api/rsvp
RSVP_ADMIN_SECRET=happywishes2026

# Required on Vercel so RSVPs persist across deploys (Vercel Blob read/write token).
# Local `next dev` writes to .data/rsvp/ without this.
# BLOB_READ_WRITE_TOKEN=
